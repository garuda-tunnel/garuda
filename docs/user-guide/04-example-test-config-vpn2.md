# Moved

This page has moved to
[`../getting-started/reference-topology.md`](../getting-started/reference-topology.md).

The old content described a flat companion-environment topology, which has been
replaced in public docs by the in-repo `examples/mini-site` reference and the
Terragrunt-based `deploy/{infra,garuda}` unit structure.
